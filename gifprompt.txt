I need to update our existing Foundry Card promo GIF.

I am attaching:
1) The current promo GIF (this must be used as the base layout).
2) All individual card PNGs to cycle through.

GOAL:
Update the GIF to match the existing layout exactly, but:
- Replace the preview panel rotation with the attached PNGs.
- Update the left “Cards” list to match the exact order I provide.
- Include ALL cards in the list.
- Do NOT modify the Highlights section.
- Do NOT redesign the layout.
- Do NOT change fonts or styling.
- Keep the original background and layout exactly the same.

ORDER:
I will provide the exact order below. Use this order for:
1) The left Cards list
2) The preview rotation sequence

[PASTE ORDER HERE]

LEFT LIST REQUIREMENTS:
- Keep the original “Cards” header.
- Do NOT overlap the Highlights section.
- Clear only the bullet list area below “Cards”.
- Ensure all items fit vertically with even spacing.
- No overlap.
- No cropping at the bottom.
- Use the exact card type names I provide (not filenames unless specified).

PREVIEW PANEL REQUIREMENTS:
- Cycle through all PNGs in the order provided.
- Title above each preview must be:
  "Card Type - Theme"
  (Use the exact filename without .png)
- Maintain original preview panel styling.
- Keep card centered and scaled proportionally.
- Keep original glow/shadow styling.

GITHUB REQUIREMENT:
- First frame must be static (longer duration ~2500ms).
- GIF must loop cleanly.
- Do not optimize in a way that breaks GitHub rendering.

OUTPUT:
- Provide final GIF for download.
- Do not change layout styling unless explicitly instructed.

Important:
This is an UPDATE to the existing promo GIF — not a redesign.
Only swap the rotating content and update the list.

Automatically calculate vertical spacing so all list items fit,
regardless of how many cards are added in the future.